# Parameters #
# Parameters are the input of function
# If the function contaions parameters then when calling the function we must have to pass the parameter value.
# If we are not passing anly value to the paramter then we will get the TypeErrror

# Create the function which print the wish massege to user with name
def wish(name):
    print(f"Hello {name} welcome to Python Programing !!!")
#Call function
wish("Adarsh")
wish("Sunny")
wish("Bunny")